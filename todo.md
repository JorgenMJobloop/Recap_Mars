# OOP problem 1
    * CanFly (creates problems on a class-basis)
        - possible solutions
            * [1] abstract property (better implementation)
            * [2] a virtual method as an action (currently implemented, has some weaknesses)
            * [3] using a interface (only animals that CAN fly, will implement the interface, that means, that all other animals that cannot fly, has no "knowledge" of the interface or it's contents)
